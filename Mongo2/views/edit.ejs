<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Chat</title>
</head>
<body>
    <p>Edit your Chat : <%= chat._id  %></p>
    <p>This chat was sent from : <b><%= chat.from %> to <b><%= chat.to %></b></b></p>
    <form action="/chats/<%= chat.id %>?_method=PUT" method="POST">
        <textarea name="msg" rows="8" cols="20"><%= chat.msg %></textarea>
        <br>
        <button>Update</button>
    </form>
</body>
</html>